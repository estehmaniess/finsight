// Basic Next.js ESM config file. Add or change options as needed.
export default {
  output: "standalone",
  reactStrictMode: true,
  // Run ESLint and TypeScript checks during build (do not ignore).
  // Example: set a custom assetPrefix or other config here.
  // assetPrefix: '',
};
